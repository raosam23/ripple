export type UserCredentialsType = {
    username?: string,
    password: string,
    email: string,
    gender?: string,
    location?: string,
    fullName?: string,
    dateOfBirth?: string,
    bio?:string,
    //TODO: Add extra data if and when required
}